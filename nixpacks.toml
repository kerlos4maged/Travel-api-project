[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql"]

[phases.build]
cmds = ["composer install --no-dev"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT${PORT:-9000}"
